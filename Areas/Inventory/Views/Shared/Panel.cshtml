@{
    ViewData["Title"] = "仓储管理";
    Layout = "_Layout";
}

<div class="flex min-h-screen">
    <nav class="w-64 bg-gray-50 p-6 border-r border-gray-200">
        <h1 class="text-2xl mb-6">仓储管理</h1>
        <ul class="space-y-2 text-lg">
            <li>
                <a href="@Url.Action("Index", "Warehouse")"
                   class="block py-2 hover:bg-gray-200 transition">
                    仓库管理
                </a>
            </li>
            <li>
                <a href="@Url.Action("Index", "Item")"
                   class="block py-2 hover:bg-gray-200 transition">
                    物品种类
                </a>
            </li>
            <li>
                <a href="@Url.Action("Index", "Transaction")"
                   class="block py-2 hover:bg-gray-200 transition">
                    出入库申请
                </a>
            </li>
        </ul>
    </nav>

    <section class="flex-1 p-6">
        @RenderBody()
    </section>
</div>